# GreenPath AI Image Assets

Place final image assets here, such as screenshots, marker art, legend imagery, or
the landing page demo poster.

The current app uses CSS-generated placeholder visuals and does not require image
files to run.
