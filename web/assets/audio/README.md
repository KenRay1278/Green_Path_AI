# GreenPath AI Audio Assets

Place final audio files here, such as route-success sounds, narration, or ambient
sound loops.

The current app uses a Web Audio generated success cue, so missing audio files will
not break the interface.
